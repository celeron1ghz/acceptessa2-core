cd $(dirname $0)/../

docker run \
    -v $PWD:/var/task \
    --entrypoint /usr/bin/perl \
    acceptessa2-mail-sender -MApp::Prove -e 'my $app = App::Prove->new; $app->process_args(@ARGV); exit( $app->run ? 0 : 1 );'